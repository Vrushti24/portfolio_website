import 'package:flutter/material.dart';

const BgColor = Color.fromARGB(255, 8, 5, 80);

const BgGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [Color(0xFF001F3D), Color(0xFF001A34)],
);

const LightGrayGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [Color(0xFFCCCCCC), Color(0xFFDDDDDD)],
);

const serviceId = 'your_serviceId';
const templateId = 'your_templateId';
const userId = 'your_userId';
const emailUrlSend = 'your_Url';
const linkedln = 'Linkedln-profile';
const github = 'github-profile';
const mail = 'mailID';
